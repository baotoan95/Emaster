server:
  port: 8080
  servlet:
    context-path: /portal

spring:
  profiles:
    active: dev

---
spring:
  profiles: dev
  
logging:
  level:
    com:
      emaster: debug

---
spring:
  profiles: prod

logging:
  level:
    com:
      emaster: info